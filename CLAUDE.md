# CLAUDE.md

## Tailwind CSS
Whenever Tailwind custom classes are defined, make sure to use their names instead of hardcoding values. For example, use `bg-primary` instead of `bg-[#00a854]` if a `primary` class exists in the Tailwind config.
