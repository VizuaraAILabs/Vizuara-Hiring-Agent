{$DOMAIN} {
    # Terminal WebSocket + HTTP
    handle_path /terminal* {
        reverse_proxy terminal:3001
    }

    # Analysis API (for admin/debug)
    handle /api/analyze/* {
        reverse_proxy analysis:8000
    }

    # Web UI — catch-all
    handle {
        reverse_proxy web:3000
    }
}
